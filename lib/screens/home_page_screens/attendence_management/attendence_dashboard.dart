import 'package:flutter/widgets.dart';

class AttendenceDashboard extends StatelessWidget {
  const AttendenceDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        const Text('Attendence Dashboard'),
      ],
    );
  }
}
